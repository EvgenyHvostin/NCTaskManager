package ua.edu.sundu.j2se.studentHvostin.tasks;

import org.junit.jupiter.api.Test;

import java.io.*;
import java.time.LocalDateTime;
import java.util.Set;
import java.util.SortedMap;

public class TestTaskIO {

    @Test
    public void testIO() throws IOException {
/*
        ArrayTaskList array = new ArrayTaskList();
        LocalDateTime time = LocalDateTime.of(1,1,1,1,1);
        InputStream in;
        File file;
        try (OutputStream out = new ObjectOutputStream()) {

            array.add(new Task("task0", time));
            array.add(new Task("task1", time));

            TaskIO.write(array, out);
        }
        TaskIO.read(array, in);
        TaskIO.writeBinary(array, file);
        TaskIO.readBinary(array, file);


        //Assertions.assertEquals(iter.next(), testTask);

 */
    }
}
