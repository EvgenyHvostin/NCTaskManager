package ua.edu.sundu.j2se.studentHvostin.tasks;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.io.*;
import java.time.LocalDateTime;
import java.util.Set;
import java.util.SortedMap;

public class TestTaskIO {

    @Test
    public void testIO() throws IOException {
/*
        AbstractTaskList tasks = new ArrayTaskList();
        AbstractTaskList newTasks = new ArrayTaskList();
        File file = new File("testTaskIO.out");

        tasks.add(new Task("task0", LocalDateTime.of(1,1,1,1,1)));
        tasks.add(new Task("task1", LocalDateTime.of(2,2,2,2,2)));

        TaskIO.writeText(tasks, file);
        TaskIO.readText(newTasks, file);

        //Assertions.assertEquals(tasks, newTasks);



        write(AbstractTaskList tasks, Writer out);
        read(AbstractTaskList tasks, Reader in);
        writeText(AbstractTaskList tasks, File file);
        readText(AbstractTaskList tasks, File file);

 */
    }
}
